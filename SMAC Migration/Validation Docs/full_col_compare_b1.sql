
WITH params AS (SELECT array('ID-000379','IN-277727') AS crews),
sac_dedup AS (
  SELECT *, ROW_NUMBER() OVER (
    PARTITION BY CREW_CODE, CAST(SIGN_ON_DATE AS DATE), CAST(SIGN_OFF_DATE AS DATE), VESSEL_NAME
    ORDER BY CAST(CONTRACT_START_DATE AS TIMESTAMP), POSITION_NAME, CAST(FROM_DATE AS TIMESTAMP)
  ) rn
  FROM reporting_layer.sac_prod_seafarer_public.revised_base_view
  WHERE CREW_CODE IN (SELECT explode(crews) FROM params)
),
smac_dedup AS (
  SELECT *, ROW_NUMBER() OVER (
    PARTITION BY CREW_CODE, CAST(SIGN_ON_DATE AS DATE), CAST(SIGN_OFF_DATE AS DATE), VESSEL_NAME
    ORDER BY CAST(CONTRACT_START_DATE AS TIMESTAMP), POSITION_NAME, CAST(FROM_DATE AS TIMESTAMP)
  ) rn
  FROM reporting_layer.smac_prod.revised_base_view
  WHERE CREW_CODE IN (SELECT explode(crews) FROM params)
)
SELECT column_name, matches, mismatches, total,
  ROUND(100.0 * matches / total, 2) AS match_pct
FROM (
  SELECT 'SEAFARER_ID' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SEAFARER_ID AS STRING),'')=COALESCE(CAST(m.SEAFARER_ID AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SEAFARER_ID AS STRING),'')<>COALESCE(CAST(m.SEAFARER_ID AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'FIRST_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.FIRST_NAME AS STRING),'')=COALESCE(CAST(m.FIRST_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.FIRST_NAME AS STRING),'')<>COALESCE(CAST(m.FIRST_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'MIDDLE_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.MIDDLE_NAME AS STRING),'')=COALESCE(CAST(m.MIDDLE_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.MIDDLE_NAME AS STRING),'')<>COALESCE(CAST(m.MIDDLE_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'LAST_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.LAST_NAME AS STRING),'')=COALESCE(CAST(m.LAST_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.LAST_NAME AS STRING),'')<>COALESCE(CAST(m.LAST_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'SEAFARER_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SEAFARER_NAME AS STRING),'')=COALESCE(CAST(m.SEAFARER_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SEAFARER_NAME AS STRING),'')<>COALESCE(CAST(m.SEAFARER_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'USER_ID' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.USER_ID AS STRING),'')=COALESCE(CAST(m.USER_ID AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.USER_ID AS STRING),'')<>COALESCE(CAST(m.USER_ID AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'AHOY_STATUS' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.AHOY_STATUS AS STRING),'')=COALESCE(CAST(m.AHOY_STATUS AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.AHOY_STATUS AS STRING),'')<>COALESCE(CAST(m.AHOY_STATUS AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'CREW_CODE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.CREW_CODE AS STRING),'')=COALESCE(CAST(m.CREW_CODE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.CREW_CODE AS STRING),'')<>COALESCE(CAST(m.CREW_CODE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'OLD_CREW_CODE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.OLD_CREW_CODE AS STRING),'')=COALESCE(CAST(m.OLD_CREW_CODE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.OLD_CREW_CODE AS STRING),'')<>COALESCE(CAST(m.OLD_CREW_CODE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'CURRENT_STATUS' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.CURRENT_STATUS AS STRING),'')=COALESCE(CAST(m.CURRENT_STATUS AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.CURRENT_STATUS AS STRING),'')<>COALESCE(CAST(m.CURRENT_STATUS AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'SEAFARER_TYPE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SEAFARER_TYPE AS STRING),'')=COALESCE(CAST(m.SEAFARER_TYPE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SEAFARER_TYPE AS STRING),'')<>COALESCE(CAST(m.SEAFARER_TYPE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'ANNIVERSARY_DATE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.ANNIVERSARY_DATE AS STRING),'')=COALESCE(CAST(m.ANNIVERSARY_DATE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.ANNIVERSARY_DATE AS STRING),'')<>COALESCE(CAST(m.ANNIVERSARY_DATE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'RANK_ID' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.RANK_ID AS STRING),'')=COALESCE(CAST(m.RANK_ID AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.RANK_ID AS STRING),'')<>COALESCE(CAST(m.RANK_ID AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'GENDER_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.GENDER_NAME AS STRING),'')=COALESCE(CAST(m.GENDER_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.GENDER_NAME AS STRING),'')<>COALESCE(CAST(m.GENDER_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'DATE_OF_BIRTH' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.DATE_OF_BIRTH AS STRING),'')=COALESCE(CAST(m.DATE_OF_BIRTH AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.DATE_OF_BIRTH AS STRING),'')<>COALESCE(CAST(m.DATE_OF_BIRTH AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'AGE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.AGE AS STRING),'')=COALESCE(CAST(m.AGE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.AGE AS STRING),'')<>COALESCE(CAST(m.AGE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'AGE_CATEGORY' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.AGE_CATEGORY AS STRING),'')=COALESCE(CAST(m.AGE_CATEGORY AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.AGE_CATEGORY AS STRING),'')<>COALESCE(CAST(m.AGE_CATEGORY AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'CREATED_AT' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.CREATED_AT AS STRING),'')=COALESCE(CAST(m.CREATED_AT AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.CREATED_AT AS STRING),'')<>COALESCE(CAST(m.CREATED_AT AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'PROFILE_STATUS' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.PROFILE_STATUS AS STRING),'')=COALESCE(CAST(m.PROFILE_STATUS AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.PROFILE_STATUS AS STRING),'')<>COALESCE(CAST(m.PROFILE_STATUS AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'ONBOARD_SAILING_STATUS' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.ONBOARD_SAILING_STATUS AS STRING),'')=COALESCE(CAST(m.ONBOARD_SAILING_STATUS AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.ONBOARD_SAILING_STATUS AS STRING),'')<>COALESCE(CAST(m.ONBOARD_SAILING_STATUS AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'AVAILABILITY_DATE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.AVAILABILITY_DATE AS STRING),'')=COALESCE(CAST(m.AVAILABILITY_DATE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.AVAILABILITY_DATE AS STRING),'')<>COALESCE(CAST(m.AVAILABILITY_DATE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'AVAILABILITY_MONTH' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.AVAILABILITY_MONTH AS STRING),'')=COALESCE(CAST(m.AVAILABILITY_MONTH AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.AVAILABILITY_MONTH AS STRING),'')<>COALESCE(CAST(m.AVAILABILITY_MONTH AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'CDC_NUMBER' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.CDC_NUMBER AS STRING),'')=COALESCE(CAST(m.CDC_NUMBER AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.CDC_NUMBER AS STRING),'')<>COALESCE(CAST(m.CDC_NUMBER AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'APPRAISAL_LINK' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.APPRAISAL_LINK AS STRING),'')=COALESCE(CAST(m.APPRAISAL_LINK AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.APPRAISAL_LINK AS STRING),'')<>COALESCE(CAST(m.APPRAISAL_LINK AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'DOCUMENTS_LINK' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.DOCUMENTS_LINK AS STRING),'')=COALESCE(CAST(m.DOCUMENTS_LINK AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.DOCUMENTS_LINK AS STRING),'')<>COALESCE(CAST(m.DOCUMENTS_LINK AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'SEA_EXPERIENCE_LINK' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SEA_EXPERIENCE_LINK AS STRING),'')=COALESCE(CAST(m.SEA_EXPERIENCE_LINK AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SEA_EXPERIENCE_LINK AS STRING),'')<>COALESCE(CAST(m.SEA_EXPERIENCE_LINK AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'SEAFARER_PROFILE_LINK' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SEAFARER_PROFILE_LINK AS STRING),'')=COALESCE(CAST(m.SEAFARER_PROFILE_LINK AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SEAFARER_PROFILE_LINK AS STRING),'')<>COALESCE(CAST(m.SEAFARER_PROFILE_LINK AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'CURRENT_RANK_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.CURRENT_RANK_NAME AS STRING),'')=COALESCE(CAST(m.CURRENT_RANK_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.CURRENT_RANK_NAME AS STRING),'')<>COALESCE(CAST(m.CURRENT_RANK_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'NATIONALITY_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.NATIONALITY_NAME AS STRING),'')=COALESCE(CAST(m.NATIONALITY_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.NATIONALITY_NAME AS STRING),'')<>COALESCE(CAST(m.NATIONALITY_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'Last DOC/Contract Company' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.`Last DOC/Contract Company` AS STRING),'')=COALESCE(CAST(m.`Last DOC/Contract Company` AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.`Last DOC/Contract Company` AS STRING),'')<>COALESCE(CAST(m.`Last DOC/Contract Company` AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'CONTACT_NUMBER' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.CONTACT_NUMBER AS STRING),'')=COALESCE(CAST(m.CONTACT_NUMBER AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.CONTACT_NUMBER AS STRING),'')<>COALESCE(CAST(m.CONTACT_NUMBER AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'EMERGENCY_CONTACT_NUMBER' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.EMERGENCY_CONTACT_NUMBER_ AS STRING),'')=COALESCE(CAST(m.EMERGENCY_CONTACT_NUMBER AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.EMERGENCY_CONTACT_NUMBER_ AS STRING),'')<>COALESCE(CAST(m.EMERGENCY_CONTACT_NUMBER AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'EMAIL_ID' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.EMAIL_ID AS STRING),'')=COALESCE(CAST(m.EMAIL_ID AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.EMAIL_ID AS STRING),'')<>COALESCE(CAST(m.EMAIL_ID AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'ADDRESS_TYPE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.ADDRESS_TYPE AS STRING),'')=COALESCE(CAST(m.ADDRESS_TYPE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.ADDRESS_TYPE AS STRING),'')<>COALESCE(CAST(m.ADDRESS_TYPE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'NEW_CONTACT_TYPE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.NEW_CONTACT_TYPE AS STRING),'')=COALESCE(CAST(m.NEW_CONTACT_TYPE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.NEW_CONTACT_TYPE AS STRING),'')<>COALESCE(CAST(m.NEW_CONTACT_TYPE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'PRIMARY_ADDRESS' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.PRIMARY_ADDRESS AS STRING),'')=COALESCE(CAST(m.PRIMARY_ADDRESS AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.PRIMARY_ADDRESS AS STRING),'')<>COALESCE(CAST(m.PRIMARY_ADDRESS AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'CITY' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.CITY AS STRING),'')=COALESCE(CAST(m.CITY AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.CITY AS STRING),'')<>COALESCE(CAST(m.CITY AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'PIN_CODE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.PIN_CODE AS STRING),'')=COALESCE(CAST(m.PIN_CODE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.PIN_CODE AS STRING),'')<>COALESCE(CAST(m.PIN_CODE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'NEAREST_AIRPORT' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.NEAREST_AIRPORT AS STRING),'')=COALESCE(CAST(m.NEAREST_AIRPORT AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.NEAREST_AIRPORT AS STRING),'')<>COALESCE(CAST(m.NEAREST_AIRPORT AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'STATE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.STATE AS STRING),'')=COALESCE(CAST(m.STATE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.STATE AS STRING),'')<>COALESCE(CAST(m.STATE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'COUNTRY' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.COUNTRY AS STRING),'')=COALESCE(CAST(m.COUNTRY AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.COUNTRY AS STRING),'')<>COALESCE(CAST(m.COUNTRY AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'SEA_EXPERIENCE_ID' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SEA_EXPERIENCE_ID AS STRING),'')=COALESCE(CAST(m.SEA_EXPERIENCE_ID AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SEA_EXPERIENCE_ID AS STRING),'')<>COALESCE(CAST(m.SEA_EXPERIENCE_ID AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'SIGN_ON_DATE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SIGN_ON_DATE AS STRING),'')=COALESCE(CAST(m.SIGN_ON_DATE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SIGN_ON_DATE AS STRING),'')<>COALESCE(CAST(m.SIGN_ON_DATE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'SIGN_OFF_DATE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SIGN_OFF_DATE AS STRING),'')=COALESCE(CAST(m.SIGN_OFF_DATE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SIGN_OFF_DATE AS STRING),'')<>COALESCE(CAST(m.SIGN_OFF_DATE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'CONTRACT_ID' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.CONTRACT_ID AS STRING),'')=COALESCE(CAST(m.CONTRACT_ID AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.CONTRACT_ID AS STRING),'')<>COALESCE(CAST(m.CONTRACT_ID AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'ACTIVE_CONTRACT' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.ACTIVE_CONTRACT AS STRING),'')=COALESCE(CAST(m.ACTIVE_CONTRACT AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.ACTIVE_CONTRACT AS STRING),'')<>COALESCE(CAST(m.ACTIVE_CONTRACT AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'SAC_CONTRACT' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SAC_CONTRACT AS STRING),'')=COALESCE(CAST(m.SAC_CONTRACT AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SAC_CONTRACT AS STRING),'')<>COALESCE(CAST(m.SAC_CONTRACT AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'VERIFIED_BY_ID' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.VERIFIED_BY_ID AS STRING),'')=COALESCE(CAST(m.VERIFIED_BY_ID AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.VERIFIED_BY_ID AS STRING),'')<>COALESCE(CAST(m.VERIFIED_BY_ID AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'IS_VERIFIED' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.IS_VERIFIED AS STRING),'')=COALESCE(CAST(m.IS_VERIFIED AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.IS_VERIFIED AS STRING),'')<>COALESCE(CAST(m.IS_VERIFIED AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'VERIFIED_BY_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.VERIFIED_BY_NAME AS STRING),'')=COALESCE(CAST(m.VERIFIED_BY_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.VERIFIED_BY_NAME AS STRING),'')<>COALESCE(CAST(m.VERIFIED_BY_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'VERIFIED_ON' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.VERIFIED_ON AS STRING),'')=COALESCE(CAST(m.VERIFIED_ON AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.VERIFIED_ON AS STRING),'')<>COALESCE(CAST(m.VERIFIED_ON AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'SIGN_OFF_REASON' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SIGN_OFF_REASON AS STRING),'')=COALESCE(CAST(m.SIGN_OFF_REASON AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SIGN_OFF_REASON AS STRING),'')<>COALESCE(CAST(m.SIGN_OFF_REASON AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'RANK_NAME_SE' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.RANK_NAME_SE AS STRING),'')=COALESCE(CAST(m.RANK_NAME_SE AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.RANK_NAME_SE AS STRING),'')<>COALESCE(CAST(m.RANK_NAME_SE AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'IMO_NUMBER' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.IMO_NUMBER AS STRING),'')=COALESCE(CAST(m.IMO_NUMBER AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.IMO_NUMBER AS STRING),'')<>COALESCE(CAST(m.IMO_NUMBER AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'VESSEL_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.VESSEL_NAME AS STRING),'')=COALESCE(CAST(m.VESSEL_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.VESSEL_NAME AS STRING),'')<>COALESCE(CAST(m.VESSEL_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'VESSEL_ID' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.VESSEL_ID AS STRING),'')=COALESCE(CAST(m.VESSEL_ID AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.VESSEL_ID AS STRING),'')<>COALESCE(CAST(m.VESSEL_ID AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'SHIP_MANAGEMENT_COMPANY_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SHIP_MANAGEMENT_COMPANY_NAME AS STRING),'')=COALESCE(CAST(m.SHIP_MANAGEMENT_COMPANY_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SHIP_MANAGEMENT_COMPANY_NAME AS STRING),'')<>COALESCE(CAST(m.SHIP_MANAGEMENT_COMPANY_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'PORT_OF_REGISTRY_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.PORT_OF_REGISTRY_NAME AS STRING),'')=COALESCE(CAST(m.PORT_OF_REGISTRY_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.PORT_OF_REGISTRY_NAME AS STRING),'')<>COALESCE(CAST(m.PORT_OF_REGISTRY_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'SHIP_MANAGEMENT_COMPANY_ID' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.SHIP_MANAGEMENT_COMPANY_ID AS STRING),'')=COALESCE(CAST(m.SHIP_MANAGEMENT_COMPANY_ID AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.SHIP_MANAGEMENT_COMPANY_ID AS STRING),'')<>COALESCE(CAST(m.SHIP_MANAGEMENT_COMPANY_ID AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn
 UNION ALL SELECT 'VESSEL_CATEGORY_NAME' AS column_name, SUM(CASE WHEN COALESCE(CAST(s.VESSEL_CATEGORY_NAME AS STRING),'')=COALESCE(CAST(m.VESSEL_CATEGORY_NAME AS STRING),'') THEN 1 ELSE 0 END) AS matches, SUM(CASE WHEN COALESCE(CAST(s.VESSEL_CATEGORY_NAME AS STRING),'')<>COALESCE(CAST(m.VESSEL_CATEGORY_NAME AS STRING),'') THEN 1 ELSE 0 END) AS mismatches, COUNT(*) AS total FROM sac_dedup s INNER JOIN smac_dedup m 
    ON s.CREW_CODE = m.CREW_CODE
   AND CAST(s.SIGN_ON_DATE AS DATE) = CAST(m.SIGN_ON_DATE AS DATE)
   AND CAST(s.SIGN_OFF_DATE AS DATE) <=> CAST(m.SIGN_OFF_DATE AS DATE)
   AND s.VESSEL_NAME <=> m.VESSEL_NAME
   AND s.rn = m.rn

)
ORDER BY match_pct ASC, column_name
